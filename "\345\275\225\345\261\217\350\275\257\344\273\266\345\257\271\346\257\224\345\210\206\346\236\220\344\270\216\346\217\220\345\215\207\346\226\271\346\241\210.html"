<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>轻量录屏软件 × Recordly 对比分析与学习提升方案</title>
<style>
  :root{
    --bg:#f4f8fc; --card:#ffffff; --ink:#1f2d3d; --sub:#5a6b7b;
    --line:#dbe6f0; --blue:#2f6fb3; --blue2:#e8f1fa; --accent:#e8772e;
    --p0:#1d9b4e; --p1:#c98a00; --p2:#9aa3ad; --red:#d23b30;
  }
  *{box-sizing:border-box}
  body{margin:0;background:var(--bg);color:var(--ink);
    font-family:"Microsoft YaHei UI","PingFang SC","Segoe UI",system-ui,sans-serif;
    line-height:1.75;font-size:15px}
  .wrap{max-width:1080px;margin:0 auto;padding:38px 26px 70px}
  header{border-bottom:3px solid var(--blue);padding-bottom:18px;margin-bottom:26px}
  h1{font-size:27px;margin:0 0 6px;color:var(--blue)}
  .sub{color:var(--sub);font-size:14px}
  h2{font-size:21px;color:var(--blue);margin:34px 0 14px;
     border-left:5px solid var(--blue);padding-left:12px}
  h3{font-size:17px;margin:20px 0 8px;color:var(--ink)}
  p{margin:8px 0}
  .card{background:var(--card);border:1px solid var(--line);border-radius:12px;
    padding:18px 20px;margin:14px 0;box-shadow:0 1px 3px rgba(47,111,179,.06)}
  .grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(230px,1fr));gap:14px}
  .kpi{background:var(--blue2);border:1px solid var(--line);border-radius:10px;padding:14px 16px}
  .kpi b{display:block;color:var(--blue);font-size:13px;margin-bottom:4px}
  table{width:100%;border-collapse:collapse;margin:12px 0;font-size:14px;
    background:var(--card);border-radius:10px;overflow:hidden;box-shadow:0 1px 3px rgba(47,111,179,.06)}
  th,td{border:1px solid var(--line);padding:10px 12px;text-align:left;vertical-align:top}
  th{background:var(--blue2);color:var(--blue);font-weight:700}
  tr:nth-child(even) td{background:#fafcfe}
  .tag{display:inline-block;padding:1px 9px;border-radius:999px;font-size:12px;font-weight:700;color:#fff;white-space:nowrap}
  .t0{background:var(--p0)} .t1{background:var(--p1)} .t2{background:var(--p2)} .tred{background:var(--red)} .tblue{background:var(--blue)}
  .pill{display:inline-block;background:#eef3f8;border:1px solid var(--line);border-radius:6px;
    padding:2px 9px;margin:3px 4px 3px 0;font-size:13px;color:var(--sub)}
  .roadmap{display:grid;grid-template-columns:repeat(3,1fr);gap:14px;margin:14px 0}
  .phase{border-radius:12px;padding:16px;border-top:4px solid}
  .ph0{border-top-color:var(--p0);background:#f1faf4}
  .ph1{border-top-color:var(--p1);background:#fdf8ec}
  .ph2{border-top-color:var(--p2);background:#f6f8fa}
  .phase h4{margin:0 0 8px;font-size:15px}
  ul{margin:6px 0 6px 2px;padding-left:20px}
  li{margin:5px 0}
  .note{background:#fff7ef;border:1px solid #f3d9bf;border-left:4px solid var(--accent);
    border-radius:8px;padding:12px 16px;margin:14px 0;font-size:14px}
  .muted{color:var(--sub);font-size:13px}
  code{background:#eef3f8;padding:1px 6px;border-radius:5px;font-size:13px;color:#234}
  footer{margin-top:40px;border-top:1px solid var(--line);padding-top:16px;color:var(--sub);font-size:13px}
</style>
</head>
<body>
<div class="wrap">

<header>
  <h1>轻量录屏软件 × Recordly：对比分析与学习提升方案</h1>
  <div class="sub">评估对象：自研 <code>screen_recorder.py</code>（Python + Tkinter + ffmpeg）　|　对标项目：<code>github.com/webadderallorg/Recordly</code>（开源录屏编辑器，AGPL-3.0）<br>
  主旨约束：<b>轻量化</b>——内核保持小巧，能力按需叠加，拒绝为追平功能而引入重运行时。</div>
</header>

<!-- ============ 1. Recordly 剖析 ============ -->
<h2>一、Recordly 项目剖析（它强在哪）</h2>
<div class="grid">
  <div class="kpi"><b>定位</b>开源屏幕录制 + 编辑器，主打"几分钟做出精美演示视频"，面向 walkthrough / 产品 demo。</div>
  <div class="kpi"><b>技术栈</b>Electron 外壳 + React/Tailwind/shadcn UI；PixiJS 场景合成；TypeScript/Vite。</div>
  <div class="kpi"><b>捕获层</b>每平台原生后端：macOS ScreenCaptureKit、Windows WGC + WASAPI、Linux Electron。</div>
  <div class="kpi"><b>编辑/渲染</b>时间线（裁剪/缩放/变速/标注）、光标美化、摄像头气泡、样式化边框背景、导出 MP4/GIF。</div>
</div>
<div class="card">
  <h3>功能特性矩阵</h3>
  <table>
    <tr><th>维度</th><th>Recordly 能力</th></tr>
    <tr><td>录制范围</td><td>全屏 / 单窗口；麦克风 + 系统音频；录后直进编辑器</td></tr>
    <tr><td>后期编辑</td><td>拖拽时间线、裁剪、手动/自动缩放、变速、文本/图像/图形标注、额外音频轨</td></tr>
    <tr><td>光标与摄像头</td><td>光标显隐/平滑/运动模糊/点击弹跳；摄像头气泡（镜像、圆角、阴影、随缩放自适应）</td></tr>
    <tr><td>边框与背景</td><td>壁纸/纯色/渐变/模糊/内边距/圆角/阴影，宽高比预设</td></tr>
    <tr><td>导出</td><td>MP4、GIF（帧率/循环/尺寸预设）、质量选择、导出后揭示文件</td></tr>
    <tr><td>工程化</td><td><code>.recordly</code> 项目文件可存可重开；社区扩展 Marketplace；自定义快捷键</td></tr>
    <tr><td>跨平台</td><td>macOS 14+ / Windows 10 19041+ / Linux；预编译 release + AUR</td></tr>
  </table>
  <p class="muted">架构：<b>捕获层 → 编辑层 → 渲染层(PixiJS) → 导出层</b> 分层解耦；原生捕获 helper 与 Electron 协调，编辑状态与源媒体路径存入项目文件。模块化、可扩展（扩展 hook 机制）是其架构精髓。</p>
</div>

<!-- ============ 2. 我们的现状 ============ -->
<h2>二、我们软件现状盘点</h2>
<div class="card">
  <table>
    <tr><th>能力</th><th>现状</th><th>说明</th></tr>
    <tr><td>录制范围</td><td class="tblue"><span class="tag tblue">有</span></td><td>全屏(主显示器) / 自定义区域 / 指定窗口(枚举可见窗口 + GetWindowRect)，高 DPI 适配</td></tr>
    <tr><td>编码质量</td><td class="tblue"><span class="tag tblue">有</span></td><td>ffmpeg gdigrab → libx264 + yuv420p(偶数裁剪)，帧率/画质可调，输出 MP4/H.264</td></tr>
    <tr><td>音频</td><td class="tblue"><span class="tag tblue">部分</span></td><td>麦克风(dshow 设备选择) + 系统声音(需"立体声混音" dshow)；本机 ffmpeg 构建不含 WASAPI</td></tr>
    <tr><td>录制流程</td><td class="tblue"><span class="tag tblue">有</span></td><td>可选延时 / 3-2-1 倒计时；任务栏隐藏(iconify) + 全局 F9 一键停止；结束 ffmpeg 校验</td></tr>
    <tr><td>形态与体积</td><td class="tblue"><span class="tag tblue">优势</span></td><td>单脚本 Python + Tkinter，PyInstaller onedir 打包，<b>内嵌 ffmpeg 免联网</b>，Windows 专属</td></tr>
    <tr><td>后期编辑</td><td class="tred"><span class="tag tred">缺</span></td><td>无裁剪/拼接/变速/标注，无光标特效、摄像头、样式化边框、GIF、项目文件</td></tr>
    <tr><td>跨平台</td><td class="tred"><span class="tag tred">缺</span></td><td>仅 Windows（gdigrab 系 Windows API），无 macOS/Linux 后端</td></tr>
  </table>
</div>

<!-- ============ 3. 五维差距 ============ -->
<h2>三、五维差距评估</h2>
<div class="card">
  <table>
    <tr><th>评估维度</th><th>我们</th><th>Recordly</th><th>差距判断</th></tr>
    <tr>
      <td><b>产品功能</b></td>
      <td>纯录制（capture-only）</td>
      <td>录制 + 一体化编辑器 + 演示增强</td>
      <td><span class="tag tred">大</span> 编辑/特效维度几乎空白，但"录制"核心已可比</td></tr>
    <tr>
      <td><b>录制性能</b></td>
      <td>gdigrab（旧式 CPU bitblt，占用偏高）</td>
      <td>WGC / ScreenCaptureKit（原生、硬件加速）</td>
      <td><span class="tag t1">中</span> WGC 可显著降低 CPU、提升画质与帧率稳定性</td></tr>
    <tr>
      <td><b>用户体验</b></td>
      <td>紧凑但偏工具化，无快捷键自定义/偏好持久化</td>
      <td>打磨过的 React UI、快捷键、偏好、即时预览</td>
      <td><span class="tag t1">中</span> 工程可用性可补，无需重做 UI 框架</td></tr>
    <tr>
      <td><b>跨平台兼容性</b></td>
      <td>Windows 专属</td>
      <td>mac / win / linux 三端原生</td>
      <td><span class="tag tred">大</span> 但非当前主旨，按需再投入</td></tr>
    <tr>
      <td><b>架构设计</b></td>
      <td>单脚本、线性流程</td>
      <td>分层解耦 + 可插拔扩展</td>
      <td><span class="tag t1">中</span> 引入"捕获抽象层 + 后期流水线"即可大幅改善</td></tr>
  </table>
  <div class="note">关键结论：差距主要在<b>录制之后</b>（编辑/特效/导出形态）与<b>捕获后端性能</b>，而非"能否录下来"。这恰好是轻量化路线可以低成本补强的部分——不必复制 Electron 重壳。</div>
</div>

<!-- ============ 4. 可借鉴核心能力点 ============ -->
<h2>四、可借鉴的核心能力点（已按轻量化过滤）</h2>
<div class="card">
  <p>以下能力对"演示/教学录屏"价值高，且多数可用 <code>ffmpeg</code> 既有 filter 或小型 native helper 实现，<b>不违背轻量主旨</b>：</p>
  <ul>
    <li><b>捕获后端可插拔</b>：抽象 <code>CaptureBackend</code> 接口，gdigrab 为默认，WGC（Win 原生 helper）为高性能备选。</li>
    <li><b>光标高亮 / 点击特效</b>：演示观感提升最大、成本适中（后期 drawbox / 捕获时叠加）。</li>
    <li><b>摄像头画中画</b>：ffmpeg 视频输入 + overlay 合成，适合口播/教程。</li>
    <li><b>样式化边框与背景</b>：圆角、阴影、内边距、模糊背景，提升成品"精致感"。</li>
    <li><b>GIF 导出</b>：palettegen/paletteuse 即可，低成本高价值。</li>
    <li><b>基础剪辑</b>：去头尾裁剪、拼接、变速——ffmpeg 后期，避开重型时间线。</li>
    <li><b>项目文件 + 重开</b>：保存设置与片段清单（对标 <code>.recordly</code>）。</li>
    <li><b>自定义快捷键 + 偏好持久化</b>：JSON 配置，记忆上次参数。</li>
    <li><b>录制后一键预览 / 打开所在文件夹</b>：缩短交付闭环。</li>
  </ul>
  <p class="muted">不建议直接借鉴：Electron 运行时、PixiJS 重渲染引擎、完整拖拽时间线编辑器——与"轻量"主旨冲突，列为 P2 权衡项。</p>
</div>

<!-- ============ 5. 具体优化方向 ============ -->
<h2>五、具体优化方向（贴合我们内核）</h2>
<div class="grid">
  <div class="card" style="margin:0">
    <h3 style="margin-top:0">捕获层</h3>
    <ul>
      <li>定义 <code>CaptureBackend</code> 抽象，gdigrab 保底</li>
      <li>Win 端评估 <b>WGC</b>（pywgc 或小型 C++ helper）替换 gdigrab → 降 CPU、升质量</li>
      <li>系统音频改 <b>WASAPI</b> 直采（换含 wasapi 的 ffmpeg 构建或加 helper）</li>
    </ul>
  </div>
  <div class="card" style="margin:0">
    <h3 style="margin-top:0">后期流水线</h3>
    <ul>
      <li>基于 ffmpeg filter 的轻量编辑：trim / concat / scale / setpts(变速) / pad(边框)</li>
      <li>光标高亮：drawbox 跟随坐标；点击波纹：短时放大圆</li>
      <li>导出矩阵：MP4 + GIF（palettegen）</li>
    </ul>
  </div>
  <div class="card" style="margin:0">
    <h3 style="margin-top:0">交互与工程</h3>
    <ul>
      <li>摄像头 dshow 视频 + overlay 画中画</li>
      <li>JSON 偏好 + 可配置快捷键（F9 之外可改键）</li>
      <li>"打开文件夹 / 预览"按钮；文件名模板</li>
      <li>项目文件 <code>.recproj</code> 存参可重开</li>
    </ul>
  </div>
</div>

<!-- ============ 6. 优先级路线图 ============ -->
<h2>六、优先级路线图</h2>
<div class="roadmap">
  <div class="phase ph0">
    <h4><span class="tag t0">P0</span> 立即可做（低成本·高价值）</h4>
    <ul>
      <li>GIF 导出（palettegen）</li>
      <li>录制后"打开所在文件夹 / 预览"</li>
      <li>自定义保存路径 + 文件名模板</li>
      <li>全局 F9 已恢复；补"可配置快捷键"</li>
      <li>实时时长 + 预计文件大小显示</li>
    </ul>
  </div>
  <div class="phase ph1">
    <h4><span class="tag t1">P1</span> 中等开发</h4>
    <ul>
      <li><b>WGC 捕获后端</b>替换 gdigrab（性能/质量）</li>
      <li>基础剪辑：去头尾 / 拼接 / 变速</li>
      <li>摄像头画中画</li>
      <li>系统音频 WASAPI 直采</li>
      <li>光标高亮 / 点击波纹</li>
    </ul>
  </div>
  <div class="phase ph2">
    <h4><span class="tag t2">P2</span> 重 / 长期（与轻量权衡）</h4>
    <ul>
      <li>样式化边框/背景/圆角/阴影、自动缩放</li>
      <li>时间线编辑器（拖动裁剪/标注）</li>
      <li>跨平台（macOS/Linux 后端）</li>
      <li>扩展 / 预设生态</li>
    </ul>
  </div>
</div>

<!-- ============ 7. 取舍原则 ============ -->
<h2>七、轻量化取舍原则（贯穿迭代）</h2>
<div class="card">
  <ul>
    <li><b>内核保持轻</b>：单文件 Python + ffmpeg，绝不引入 Electron / 大型运行时——这是与 Recordly 最根本的分野，也是我们的竞争力。</li>
    <li><b>能力按需叠加</b>：把"重"能力（完整时间线编辑、跨平台）做成可选模块，不进入核心路径。</li>
    <li><b>优先复用 ffmpeg filter</b>：特效尽量用既有滤镜实现，避免自研重编码。</li>
    <li><b>捕获仍走 native API</b>：性能敏感处用 WGC / ScreenCaptureKit，但封装成可插拔后端，不影响轻量主体。</li>
    <li><b>对标"演示增强"而非"全功能剪辑"</b>：在录制 + 轻后期 + 精致导出这条线上做深，而非去追平专业 NLE。</li>
  </ul>
</div>

<div class="note">
  <b>本轮已落地的一项（任务 #17）：</b>弃用不稳定的系统托盘（Shell_NotifyIcon），恢复"最小化到任务栏 + 全局 F9 低级别键盘钩子"方案——窗口收起后按 F9 仍可一键停止，简单可靠、零托盘依赖；打包由 <code>--onefile</code> 改为 <code>--onedir</code>（任务 #18），规避 onefile 退出时清理临时目录的 <code>PermissionError [WinError 5]</code> 报错。
</div>

<footer>
  分析依据：Recordly 公开仓库 README / 代码结构梳理（2026-08）；自研软件以 <code>screen_recorder.py</code> 当前实现为准。
  本方案为迭代指导文档，优先级可按实际资源与用户反馈滚动调整。
</footer>

</div>
</body>
</html>
